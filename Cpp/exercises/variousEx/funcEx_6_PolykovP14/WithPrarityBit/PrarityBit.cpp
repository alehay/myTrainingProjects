

// binary conversion decimal number
void binaryConversion (const int *binNumnArr, decNum ){

    if (!array)
    return;

// const - for protect adress for chenged
// protect If user passed null pointer as array

    for(i=0; decNum>0; i++){
    binNumnArr[i]=decNum%2;
    decNum= decNum/2;
    }
}
